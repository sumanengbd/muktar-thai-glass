// Auto-synced setup SQL (no staff PIN). Super Admin clipboard only.
// Schema SQL is safe to re-run — never wipes companies/aluminium/locks/quotes/staff.
window.MTG_SQL_SETUP = {
  schema: "-- MTG / Glass House — Supabase schema\n-- SAFE TO RE-RUN after feature updates (Admin → Database → Schema SQL → Run).\n-- This script only creates/replaces tables, indexes, policies & RPCs.\n-- It NEVER deletes companies, aluminium, locks, quotes, or staff rows.\n-- Catalog is owned by Admin UI; running Schema SQL does not wipe shop rates.\n-- Run in: Supabase Dashboard → SQL Editor → New query → Run\n\ncreate extension if not exists pgcrypto;\n\n-- ---------- tables ----------\ncreate table if not exists public.staff_users (\n  email text primary key,\n  pin text not null,\n  role text not null check (role in ('superadmin', 'owner', 'operator')),\n  created_at timestamptz not null default now()\n);\n\n-- Exactly one Super Admin allowed (set only via SQL Editor, not the app)\ncreate unique index if not exists staff_users_one_superadmin\n  on public.staff_users (role)\n  where role = 'superadmin';\n\ncreate table if not exists public.companies (\n  id bigserial primary key,\n  name text not null,\n  thickness numeric not null default 5,\n  rate numeric not null default 0,\n  color text not null default 'clear',\n  out boolean not null default false,\n  sort_order int not null default 0\n);\n\ncreate table if not exists public.aluminium (\n  id bigserial primary key,\n  name text not null,\n  thickness numeric not null default 1,\n  rate numeric not null default 0,\n  parts jsonb not null default '{}'::jsonb,\n  color text not null default 'silver',\n  sort_order int not null default 0\n);\n\ncreate table if not exists public.locks (\n  id bigserial primary key,\n  name text not null,\n  rate numeric not null default 0,\n  style text not null default 'generic',\n  sort_order int not null default 0\n);\n\ncreate table if not exists public.shop (\n  id int primary key default 1 check (id = 1),\n  name text not null default '',\n  slogan text not null default '',\n  logo text not null default '',\n  phone text not null default '',\n  address text not null default ''\n);\n\ncreate table if not exists public.charges (\n  id int primary key default 1 check (id = 1),\n  net numeric not null default 0,\n  extra numeric not null default 0\n);\n\ncreate table if not exists public.cut_params (\n  id int primary key default 1 check (id = 1),\n  outer_horiz numeric not null default 0.4,\n  side numeric not null default 1.1,\n  shutter_horiz numeric not null default 6,\n  glass_h numeric not null default 4.2,\n  glass_w numeric not null default 5\n);\n\ncreate table if not exists public.quotes (\n  id text primary key,\n  created_at timestamptz not null default now(),\n  name text not null,\n  phone text not null,\n  status text not null default 'new',\n  assigned text not null default '',\n  items_count int not null default 0,\n  sqft numeric not null default 0,\n  total numeric not null default 0\n);\n\ncreate table if not exists public.quote_lines (\n  id bigserial primary key,\n  quote_id text not null references public.quotes(id) on delete cascade,\n  type_label text not null default '',\n  size_label text not null default '',\n  qty int not null default 1,\n  color text not null default '',\n  company text not null default '',\n  lock_name text not null default '',\n  net boolean not null default false,\n  line_total numeric not null default 0,\n  aluminium text not null default '',\n  alu_mm text not null default '',\n  glass_mm text not null default '',\n  alu_color text not null default '',\n  type_id text not null default '',\n  height_ft numeric not null default 0,\n  width_ft numeric not null default 0,\n  lock_style text not null default '',\n  room text not null default ''\n);\n\ncreate index if not exists quote_lines_quote_id_idx on public.quote_lines(quote_id);\ncreate index if not exists quotes_created_at_idx on public.quotes(created_at desc);\n\n-- ---------- RLS ----------\nalter table public.staff_users enable row level security;\nalter table public.companies enable row level security;\nalter table public.aluminium enable row level security;\nalter table public.locks enable row level security;\nalter table public.shop enable row level security;\nalter table public.charges enable row level security;\nalter table public.charges add column if not exists season_pct numeric not null default 0;\nalter table public.aluminium add column if not exists parts jsonb not null default '{}'::jsonb;\nalter table public.cut_params enable row level security;\nalter table public.quotes enable row level security;\nalter table public.quote_lines enable row level security;\n\n-- Public can read catalog (calculator visitors)\ndrop policy if exists companies_read on public.companies;\ncreate policy companies_read on public.companies for select using (true);\ndrop policy if exists aluminium_read on public.aluminium;\ncreate policy aluminium_read on public.aluminium for select using (true);\ndrop policy if exists locks_read on public.locks;\ncreate policy locks_read on public.locks for select using (true);\ndrop policy if exists shop_read on public.shop;\ncreate policy shop_read on public.shop for select using (true);\ndrop policy if exists charges_read on public.charges;\ncreate policy charges_read on public.charges for select using (true);\ndrop policy if exists cut_params_read on public.cut_params;\ncreate policy cut_params_read on public.cut_params for select using (true);\n\n-- No direct anon writes; all writes go through SECURITY DEFINER RPCs\n-- staff_users / quotes: no public select (quotes via RPC)\n\n-- ---------- helpers ----------\ncreate or replace function public.mtg_auth(p_email text, p_pin text)\nreturns table(email text, role text)\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\n#variable_conflict use_column\ndeclare\n  em text := lower(trim(coalesce(p_email, '')));\n  sa text := 'sumanengbd@gmail.com';\nbegin\n  -- Forever Super Admin: keep this email as the only superadmin after valid PIN\n  if em = sa and exists (\n    select 1 from public.staff_users su\n    where lower(trim(su.email)) = sa and su.pin = trim(p_pin)\n  ) then\n    update public.staff_users su\n      set role = 'owner'\n      where su.role = 'superadmin' and lower(trim(su.email)) is distinct from sa;\n    update public.staff_users su\n      set role = 'superadmin'\n      where lower(trim(su.email)) = sa;\n  end if;\n\n  return query\n  select su.email,\n    case when lower(trim(su.email)) = sa then 'superadmin'::text else su.role end\n  from public.staff_users su\n  where lower(trim(su.email)) = em\n    and su.pin = trim(p_pin)\n    and su.role in ('superadmin', 'owner', 'operator')\n  limit 1;\nend;\n$$;\n\ncreate or replace function public.mtg_glass_thicks()\nreturns numeric[]\nlanguage sql\nstable\nsecurity definer\nset search_path = public\nas $$\n  select coalesce(array_agg(distinct thickness order by thickness), '{}'::numeric[])\n  from public.companies\n  where thickness > 0;\n$$;\n\ncreate or replace function public.mtg_read_quotes()\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  out jsonb := '[]'::jsonb;\nbegin\n  select coalesce(jsonb_agg(q order by q->>'time' desc), '[]'::jsonb)\n  into out\n  from (\n    select jsonb_build_object(\n      'id', h.id,\n      'time', to_char(h.created_at at time zone 'Asia/Dhaka', 'YYYY-MM-DD HH24:MI'),\n      'name', h.name,\n      'phone', h.phone,\n      'status', h.status,\n      'assigned', h.assigned,\n      'items', h.items_count,\n      'sqft', h.sqft,\n      'total', h.total,\n      'lines', coalesce((\n        select jsonb_agg(jsonb_build_object(\n          'type', l.type_label,\n          'size', l.size_label,\n          'qty', l.qty,\n          'color', l.color,\n          'company', l.company,\n          'lock', l.lock_name,\n          'net', l.net,\n          'total', l.line_total,\n          'aluminium', l.aluminium,\n          'aluMm', l.alu_mm,\n          'glassMm', l.glass_mm,\n          'aluColor', l.alu_color,\n          'typeId', l.type_id,\n          'heightFt', l.height_ft,\n          'widthFt', l.width_ft,\n          'lockStyle', l.lock_style,\n          'room', l.room\n        ) order by l.id)\n        from public.quote_lines l where l.quote_id = h.id\n      ), '[]'::jsonb)\n    ) as q\n    from public.quotes h\n    order by h.created_at desc\n    limit 80\n  ) s;\n  return out;\nend;\n$$;\n\ncreate or replace function public.mtg_catalog()\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  shop_row public.shop%rowtype;\n  ch public.charges%rowtype;\n  cp public.cut_params%rowtype;\nbegin\n  select * into shop_row from public.shop where id = 1;\n  select * into ch from public.charges where id = 1;\n  select * into cp from public.cut_params where id = 1;\n  return jsonb_build_object(\n    'ok', true,\n    'companies', coalesce((\n      select jsonb_agg(jsonb_build_object(\n        'name', c.name, 'thickness', c.thickness, 'rate', c.rate,\n        'color', c.color, 'out', c.out\n      ) order by c.sort_order, c.id)\n      from public.companies c\n    ), '[]'::jsonb),\n    'aluminium', coalesce((\n      select jsonb_agg(jsonb_build_object(\n        'name', a.name, 'thickness', a.thickness, 'rate', a.rate, 'parts', coalesce(a.parts, '{}'::jsonb), 'color', a.color\n      ) order by a.sort_order, a.id)\n      from public.aluminium a\n    ), '[]'::jsonb),\n    'locks', coalesce((\n      select jsonb_agg(jsonb_build_object(\n        'name', l.name, 'rate', l.rate, 'style', l.style\n      ) order by l.sort_order, l.id)\n      from public.locks l\n    ), '[]'::jsonb),\n    'glassThicks', to_jsonb(public.mtg_glass_thicks()),\n    'charges', jsonb_build_object('net', coalesce(ch.net, 0), 'extra', coalesce(ch.extra, 0), 'seasonPct', coalesce(ch.season_pct, 0)),\n    'cutParams', jsonb_build_object(\n      'outerHoriz', coalesce(cp.outer_horiz, 0.4),\n      'side', coalesce(cp.side, 1.1),\n      'shutterHoriz', coalesce(cp.shutter_horiz, 6),\n      'glassH', coalesce(cp.glass_h, 4.2),\n      'glassW', coalesce(cp.glass_w, 5)\n    ),\n    'shop', jsonb_build_object(\n      'name', coalesce(shop_row.name, ''),\n      'slogan', coalesce(shop_row.slogan, ''),\n      'logo', coalesce(shop_row.logo, ''),\n      'phone', coalesce(shop_row.phone, ''),\n      'address', coalesce(shop_row.address, '')\n    )\n  );\nend;\n$$;\n\ncreate or replace function public.mtg_unlock(p_email text, p_pin text)\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  u record;\n  cat jsonb;\nbegin\n  select * into u from public.mtg_auth(p_email, p_pin);\n  if u.email is null then\n    return jsonb_build_object('ok', false, 'error', 'pin');\n  end if;\n  cat := public.mtg_catalog();\n  cat := cat || jsonb_build_object(\n    'ok', true,\n    'role', u.role,\n    'quotes', case when u.role in ('superadmin', 'owner', 'operator') then public.mtg_read_quotes() else '[]'::jsonb end,\n    'users', case when u.role in ('superadmin', 'owner') then coalesce((\n      select jsonb_agg(jsonb_build_object('email', s.email, 'role', s.role) order by s.email)\n      from public.staff_users s\n    ), '[]'::jsonb) else '[]'::jsonb end\n  );\n  return cat;\nend;\n$$;\n\ncreate or replace function public.mtg_save(\n  p_email text,\n  p_pin text,\n  p_sections jsonb,\n  p_payload jsonb\n)\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  u record;\n  sections text[];\n  write_all boolean;\n  i int;\n  arr jsonb;\n  el jsonb;\n  shop jsonb;\n  ch jsonb;\n  cp jsonb;\n  keep_emails text[];\n  em text;\n  new_pin text;\n  new_role text;\nbegin\n  select * into u from public.mtg_auth(p_email, p_pin);\n  if u.email is null then\n    return jsonb_build_object('ok', false, 'error', 'pin');\n  end if;\n  if u.role not in ('superadmin', 'owner') then\n    return jsonb_build_object('ok', false, 'error', 'forbidden');\n  end if;\n\n  select coalesce(array_agg(x), '{}') into sections\n  from jsonb_array_elements_text(coalesce(p_sections, '[]'::jsonb)) as t(x);\n\n  write_all := coalesce(array_length(sections, 1), 0) = 0;\n\n  if write_all or 'companies' = any(sections) then\n    arr := coalesce(p_payload->'companies', '[]'::jsonb);\n    -- Empty payload must NOT wipe catalog (Schema re-run / bad client safe)\n    if jsonb_array_length(arr) > 0 then\n      delete from public.companies where true;\n      for i in 0 .. greatest(jsonb_array_length(arr) - 1, -1) loop\n        el := arr->i;\n        if coalesce(nullif(trim(el->>'name'), ''), '') = '' then continue; end if;\n        insert into public.companies(name, thickness, rate, color, out, sort_order)\n        values (\n          trim(el->>'name'),\n          coalesce((el->>'thickness')::numeric, 5),\n          coalesce((el->>'rate')::numeric, 0),\n          coalesce(nullif(trim(el->>'color'), ''), 'clear'),\n          coalesce((el->>'out')::boolean, false),\n          i\n        );\n      end loop;\n    end if;\n  end if;\n\n  if write_all or 'aluminium' = any(sections) then\n    arr := coalesce(p_payload->'aluminium', '[]'::jsonb);\n    if jsonb_array_length(arr) > 0 then\n      delete from public.aluminium where true;\n      for i in 0 .. greatest(jsonb_array_length(arr) - 1, -1) loop\n        el := arr->i;\n        if coalesce(nullif(trim(el->>'name'), ''), '') = '' then continue; end if;\n        insert into public.aluminium(name, thickness, rate, parts, color, sort_order)\n        values (\n          trim(el->>'name'),\n          coalesce((el->>'thickness')::numeric, 1),\n          coalesce((el->>'rate')::numeric, 0),\n          coalesce(el->'parts', '{}'::jsonb),\n          coalesce(nullif(trim(el->>'color'), ''), 'silver'),\n          i\n        );\n      end loop;\n    end if;\n  end if;\n\n  if write_all or 'locks' = any(sections) then\n    arr := coalesce(p_payload->'locks', '[]'::jsonb);\n    if jsonb_array_length(arr) > 0 then\n      delete from public.locks where true;\n      for i in 0 .. greatest(jsonb_array_length(arr) - 1, -1) loop\n        el := arr->i;\n        if coalesce(nullif(trim(el->>'name'), ''), '') = '' then continue; end if;\n        insert into public.locks(name, rate, style, sort_order)\n        values (\n          trim(el->>'name'),\n          coalesce((el->>'rate')::numeric, 0),\n          coalesce(nullif(trim(el->>'style'), ''), 'generic'),\n          i\n        );\n      end loop;\n    end if;\n  end if;\n\n  if write_all or 'charges' = any(sections) then\n    ch := coalesce(p_payload->'charges', '{}'::jsonb);\n    insert into public.charges(id, net, extra)\n    values (1, coalesce((ch->>'net')::numeric, 0), coalesce((ch->>'extra')::numeric, 0))\n    on conflict (id) do update set\n      net = excluded.net,\n      extra = excluded.extra;\n  end if;\n\n  if write_all or 'cut' = any(sections) or 'cutParams' = any(sections) then\n    cp := coalesce(p_payload->'cutParams', '{}'::jsonb);\n    insert into public.cut_params(id, outer_horiz, side, shutter_horiz, glass_h, glass_w)\n    values (\n      1,\n      coalesce((cp->>'outerHoriz')::numeric, 0.4),\n      coalesce((cp->>'side')::numeric, 1.1),\n      coalesce((cp->>'shutterHoriz')::numeric, 6),\n      coalesce((cp->>'glassH')::numeric, 4.2),\n      coalesce((cp->>'glassW')::numeric, 5)\n    )\n    on conflict (id) do update set\n      outer_horiz = excluded.outer_horiz,\n      side = excluded.side,\n      shutter_horiz = excluded.shutter_horiz,\n      glass_h = excluded.glass_h,\n      glass_w = excluded.glass_w;\n  end if;\n\n  if write_all or 'shop' = any(sections) then\n    shop := coalesce(p_payload->'shop', '{}'::jsonb);\n    insert into public.shop(id, name, slogan, logo, phone, address)\n    values (\n      1,\n      left(coalesce(shop->>'name', ''), 80),\n      left(coalesce(shop->>'slogan', ''), 140),\n      left(coalesce(shop->>'logo', ''), 50000),\n      left(coalesce(shop->>'phone', ''), 40),\n      left(coalesce(shop->>'address', ''), 180)\n    )\n    on conflict (id) do update set\n      name = excluded.name,\n      slogan = excluded.slogan,\n      logo = excluded.logo,\n      phone = excluded.phone,\n      address = excluded.address;\n  end if;\n\n  if (write_all or 'users' = any(sections)) and jsonb_typeof(p_payload->'users') = 'array' then\n    arr := coalesce(p_payload->'users', '[]'::jsonb);\n    keep_emails := array[lower(trim(p_email)), 'sumanengbd@gmail.com'];\n\n    for i in 0 .. greatest(jsonb_array_length(arr) - 1, -1) loop\n      el := arr->i;\n      em := lower(trim(coalesce(el->>'email', '')));\n      if position('@' in em) < 2 then continue; end if;\n\n      -- Super Admin: pin change only by Super Admin; never delete/demote via app\n      if exists (\n        select 1 from public.staff_users s\n        where lower(trim(s.email)) = em and s.role = 'superadmin'\n      ) or em = 'sumanengbd@gmail.com' then\n        keep_emails := array_append(keep_emails, em);\n        if u.role = 'superadmin' then\n          new_pin := trim(coalesce(el->>'pin', ''));\n          if new_pin ~ '^\\d{4,15}$' then\n            update public.staff_users\n              set pin = new_pin, role = 'superadmin'\n              where lower(trim(email)) = em;\n          end if;\n        end if;\n        continue;\n      end if;\n      if el->>'role' = 'superadmin' then continue; end if;\n\n      new_pin := trim(coalesce(el->>'pin', ''));\n      new_role := case when el->>'role' = 'owner' then 'owner' else 'operator' end;\n      keep_emails := array_append(keep_emails, em);\n\n      if new_pin ~ '^\\d{4,15}$' then\n        insert into public.staff_users(email, pin, role)\n        values (em, new_pin, new_role)\n        on conflict (email) do update set\n          pin = excluded.pin,\n          role = case\n            when public.staff_users.role = 'superadmin' then 'superadmin'\n            else excluded.role\n          end;\n      elsif exists (select 1 from public.staff_users s where s.email = em) then\n        update public.staff_users\n          set role = new_role\n          where email = em and role is distinct from 'superadmin';\n      end if;\n    end loop;\n\n    delete from public.staff_users s\n    where s.role is distinct from 'superadmin'\n      and lower(trim(s.email)) is distinct from 'sumanengbd@gmail.com'\n      and s.email <> lower(trim(p_email))\n      and not (s.email = any (keep_emails));\n\n    update public.staff_users set role = 'owner'\n      where role = 'superadmin' and lower(trim(email)) is distinct from 'sumanengbd@gmail.com';\n    update public.staff_users set role = 'superadmin'\n      where lower(trim(email)) = 'sumanengbd@gmail.com';\n\n    insert into public.staff_users(email, pin, role)\n    values (lower(trim(p_email)), trim(p_pin), 'owner')\n    on conflict (email) do update set\n      pin = excluded.pin,\n      role = case\n        when public.staff_users.role = 'superadmin' then 'superadmin'\n        when public.staff_users.role = 'owner' then 'owner'\n        else 'owner'\n      end;\n  end if;\n\n  return jsonb_build_object(\n    'ok', true,\n    'role', coalesce((select s.role from public.staff_users s where s.email = lower(trim(p_email))), 'owner'),\n    'users', coalesce((\n      select jsonb_agg(jsonb_build_object('email', s.email, 'role', s.role) order by s.email)\n      from public.staff_users s\n    ), '[]'::jsonb)\n  );\nend;\n$$;\n\ncreate or replace function public.mtg_quote(p_name text, p_phone text, p_items jsonb)\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  qid text;\n  phone text := regexp_replace(coalesce(p_phone, ''), '\\s+', '', 'g');\n  name text := trim(coalesce(p_name, ''));\n  n int := coalesce(jsonb_array_length(p_items), 0);\n  i int;\n  el jsonb;\n  h numeric;\n  w numeric;\n  line_qty int;\n  sq numeric := 0;\n  tot numeric := 0;\n  line_tot numeric;\n  size_label text;\nbegin\n  if char_length(name) < 2 or char_length(name) > 60 then\n    return jsonb_build_object('ok', false, 'error', 'name');\n  end if;\n  if not (phone ~ '^(?:\\+?88)?01\\d{9}$' or phone ~ '^\\d{10,15}$') then\n    return jsonb_build_object('ok', false, 'error', 'phone');\n  end if;\n  if n < 1 or n > 40 then\n    return jsonb_build_object('ok', false, 'error', 'items');\n  end if;\n\n  qid := 'Q' || to_char(now() at time zone 'Asia/Dhaka', 'YYYYMMDD') || '-' ||\n         upper(substr(replace(gen_random_uuid()::text, '-', ''), 1, 4));\n\n  for i in 0 .. n - 1 loop\n    el := p_items->i;\n    h := coalesce((el->>'heightFt')::numeric, 0);\n    w := coalesce((el->>'widthFt')::numeric, 0);\n    line_qty := least(99, greatest(1, coalesce((el->>'qty')::int, 1)));\n    if h <= 0 or w <= 0 or h > 40 or w > 40 then continue; end if;\n    line_tot := coalesce((el->>'sqft')::numeric, h * w * line_qty);\n    if (el ? 'total') then line_tot := coalesce((el->>'total')::numeric, line_tot); end if;\n    sq := sq + (h * w * line_qty);\n    tot := tot + coalesce((el->>'subtotal')::numeric, (el->>'total')::numeric, h * w * line_qty);\n  end loop;\n\n  insert into public.quotes(id, name, phone, status, items_count, sqft, total)\n  values (qid, name, phone, 'new', n, round(sq::numeric, 2), round(tot::numeric, 2));\n\n  for i in 0 .. n - 1 loop\n    el := p_items->i;\n    h := coalesce((el->>'heightFt')::numeric, 0);\n    w := coalesce((el->>'widthFt')::numeric, 0);\n    line_qty := least(99, greatest(1, coalesce((el->>'qty')::int, 1)));\n    if h <= 0 or w <= 0 or h > 40 or w > 40 then continue; end if;\n    size_label := coalesce(nullif(trim(el->>'size'), ''),\n      trim(to_char(h, 'FM999990.##')) || ' x ' || trim(to_char(w, 'FM999990.##')));\n    insert into public.quote_lines(\n      quote_id, type_label, size_label, qty, color, company, lock_name, net, line_total,\n      aluminium, alu_mm, glass_mm, alu_color, type_id, height_ft, width_ft, lock_style, room\n    ) values (\n      qid,\n      coalesce(el->>'type', ''),\n      size_label,\n      line_qty,\n      coalesce(el->>'color', ''),\n      coalesce(el->>'company', ''),\n      coalesce(nullif(el->>'lock', ''), '-'),\n      coalesce((el->>'net')::boolean, false),\n      coalesce((el->>'subtotal')::numeric, (el->>'total')::numeric, h * w * line_qty),\n      coalesce(el->>'aluminium', ''),\n      coalesce(el->>'aluThick', el->>'alu_mm', ''),\n      coalesce(el->>'thickness', el->>'glass_mm', ''),\n      coalesce(el->>'aluColor', el->>'aluColorLabel', ''),\n      coalesce(el->>'typeId', el->>'winType', ''),\n      h, w,\n      coalesce(el->>'lockStyle', ''),\n      left(coalesce(el->>'room', ''), 40)\n    );\n  end loop;\n\n  update public.quotes set\n    items_count = (select count(*) from public.quote_lines ql where ql.quote_id = qid),\n    sqft = round((select coalesce(sum(ql.height_ft * ql.width_ft * ql.qty), 0) from public.quote_lines ql where ql.quote_id = qid), 2),\n    total = round((select coalesce(sum(ql.line_total), 0) from public.quote_lines ql where ql.quote_id = qid), 2)\n  where id = qid;\n\n  return jsonb_build_object(\n    'ok', true,\n    'id', qid,\n    'name', name,\n    'phone', phone,\n    'sqft', (select sqft from public.quotes where id = qid),\n    'total', (select total from public.quotes where id = qid)\n  );\nend;\n$$;\n\ncreate or replace function public.mtg_quote_patch(\n  p_email text,\n  p_pin text,\n  p_id text,\n  p_status text default null,\n  p_assigned text default null\n)\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  u record;\n  st text;\n  asg text;\nbegin\n  select * into u from public.mtg_auth(p_email, p_pin);\n  if u.email is null then\n    return jsonb_build_object('ok', false, 'error', 'pin');\n  end if;\n  if u.role not in ('superadmin', 'owner', 'operator') then\n    return jsonb_build_object('ok', false, 'error', 'forbidden');\n  end if;\n  if not exists (select 1 from public.quotes where id = trim(p_id)) then\n    return jsonb_build_object('ok', false, 'error', 'notfound');\n  end if;\n  if p_status is not null then\n    st := lower(trim(p_status));\n    if st not in ('new', 'called', 'booked', 'delivered') then st := 'new'; end if;\n    update public.quotes set status = st where id = trim(p_id);\n  end if;\n  if p_assigned is not null then\n    asg := lower(trim(p_assigned));\n    if asg <> '' and position('@' in asg) < 2 then asg := ''; end if;\n    update public.quotes set assigned = asg where id = trim(p_id);\n  end if;\n  return jsonb_build_object('ok', true, 'quotes', public.mtg_read_quotes());\nend;\n$$;\n\ncreate or replace function public.mtg_quote_public(p_id text)\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  q jsonb;\nbegin\n  select jsonb_build_object(\n    'ok', true,\n    'quote', jsonb_build_object(\n      'id', h.id,\n      'time', to_char(h.created_at at time zone 'Asia/Dhaka', 'YYYY-MM-DD HH24:MI'),\n      'name', h.name,\n      'phone', h.phone,\n      'status', h.status,\n      'items', h.items_count,\n      'sqft', h.sqft,\n      'total', h.total,\n      'lines', coalesce((\n        select jsonb_agg(jsonb_build_object(\n          'type', l.type_label,\n          'size', l.size_label,\n          'qty', l.qty,\n          'color', l.color,\n          'company', l.company,\n          'lock', l.lock_name,\n          'net', l.net,\n          'total', l.line_total,\n          'aluminium', l.aluminium,\n          'aluMm', l.alu_mm,\n          'glassMm', l.glass_mm,\n          'aluColor', l.alu_color,\n          'typeId', l.type_id,\n          'heightFt', l.height_ft,\n          'widthFt', l.width_ft,\n          'lockStyle', l.lock_style,\n          'room', l.room\n        ) order by l.id)\n        from public.quote_lines l where l.quote_id = h.id\n      ), '[]'::jsonb)\n    )\n  )\n  into q\n  from public.quotes h\n  where h.id = trim(p_id)\n  limit 1;\n\n  if q is null then\n    return jsonb_build_object('ok', false, 'error', 'notfound');\n  end if;\n  return q;\nend;\n$$;\n\ngrant execute on function public.mtg_catalog() to anon, authenticated;\ngrant execute on function public.mtg_unlock(text, text) to anon, authenticated;\ngrant execute on function public.mtg_save(text, text, jsonb, jsonb) to anon, authenticated;\ngrant execute on function public.mtg_quote(text, text, jsonb) to anon, authenticated;\ngrant execute on function public.mtg_quote_patch(text, text, text, text, text) to anon, authenticated;\ngrant execute on function public.mtg_quote_public(text) to anon, authenticated;\n\n-- ---------- seed (safe to re-run — never wipes catalog / quotes / staff) ----------\n-- Upgrade role check if table already existed with older constraint\nalter table public.staff_users drop constraint if exists staff_users_role_check;\nalter table public.staff_users\n  add constraint staff_users_role_check\n  check (role in ('superadmin', 'owner', 'operator'));\n\ncreate unique index if not exists staff_users_one_superadmin\n  on public.staff_users (role)\n  where role = 'superadmin';\n\n-- Staff login seed (NO PIN in public files).\n-- Forever Super Admin: sumanengbd@gmail.com (role only — PIN never in public files).\nupdate public.staff_users set role = 'owner'\n  where role = 'superadmin' and lower(trim(email)) is distinct from 'sumanengbd@gmail.com';\nupdate public.staff_users set role = 'superadmin'\n  where lower(trim(email)) = 'sumanengbd@gmail.com';\n-- App staff form can add Owner + Staff only.\n\n-- First-time row only; existing shop/charges/cut_params are kept as-is\ninsert into public.shop(id, name, slogan, logo, phone, address)\nvalues (\n  1,\n  'Suman Thai Glass',\n  'Thai glass window & door calculator — see the size and estimate price with visuals',\n  'images/logo.svg',\n  '+8801727282829',\n  'Walia Bazar, Lalpur, Natore'\n)\non conflict (id) do nothing;\n\ninsert into public.charges(id, net, extra) values (1, 200, 0)\non conflict (id) do nothing;\n\ninsert into public.cut_params(id) values (1)\non conflict (id) do nothing;\n\n-- Catalog / quotes / staff rows are NEVER deleted by this Schema script —\n-- even when you re-run Schema SQL after feature updates.\n-- companies / aluminium / locks / quotes stay owned by Admin UI.\n\n-- Public: find quotes by mobile (visitor lookup)\ncreate or replace function public.mtg_quote_by_phone(p_phone text)\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  digits text := regexp_replace(coalesce(p_phone, ''), '\\D', '', 'g');\n  needle text;\n  out jsonb;\nbegin\n  if char_length(digits) < 10 or char_length(digits) > 15 then\n    return jsonb_build_object('ok', false, 'error', 'phone');\n  end if;\n\n  -- Match last 10 digits (BD mobile without country / leading 0)\n  needle := right(digits, 10);\n\n  select coalesce(jsonb_agg(q order by q->>'time' desc), '[]'::jsonb)\n  into out\n  from (\n    select jsonb_build_object(\n      'id', h.id,\n      'time', to_char(h.created_at at time zone 'Asia/Dhaka', 'YYYY-MM-DD HH24:MI'),\n      'name', h.name,\n      'phone', h.phone,\n      'status', h.status,\n      'items', h.items_count,\n      'sqft', h.sqft,\n      'total', h.total,\n      'lines', coalesce((\n        select jsonb_agg(jsonb_build_object(\n          'type', l.type_label,\n          'size', l.size_label,\n          'qty', l.qty,\n          'color', l.color,\n          'company', l.company,\n          'lock', l.lock_name,\n          'net', l.net,\n          'total', l.line_total,\n          'aluminium', l.aluminium,\n          'aluMm', l.alu_mm,\n          'glassMm', l.glass_mm,\n          'aluColor', l.alu_color,\n          'typeId', l.type_id,\n          'heightFt', l.height_ft,\n          'widthFt', l.width_ft,\n          'lockStyle', l.lock_style,\n          'room', l.room\n        ) order by l.id)\n        from public.quote_lines l where l.quote_id = h.id\n      ), '[]'::jsonb)\n    ) as q\n    from public.quotes h\n    where right(regexp_replace(h.phone, '\\D', '', 'g'), 10) = needle\n    order by h.created_at desc\n    limit 20\n  ) s;\n\n  return jsonb_build_object('ok', true, 'quotes', out, 'count', jsonb_array_length(out));\nend;\n$$;\n\ngrant execute on function public.mtg_quote_by_phone(text) to anon, authenticated;\n\n-- Owner / Super Admin only: permanently delete a visitor quote (+ cascade lines)\ncreate or replace function public.mtg_quote_delete(\n  p_email text,\n  p_pin text,\n  p_id text\n)\nreturns jsonb\nlanguage plpgsql\nsecurity definer\nset search_path = public\nas $$\ndeclare\n  u record;\n  qid text := trim(coalesce(p_id, ''));\nbegin\n  select * into u from public.mtg_auth(p_email, p_pin);\n  if u.email is null then\n    return jsonb_build_object('ok', false, 'error', 'pin');\n  end if;\n  if u.role not in ('superadmin', 'owner') then\n    return jsonb_build_object('ok', false, 'error', 'forbidden');\n  end if;\n  if qid = '' or not exists (select 1 from public.quotes where id = qid) then\n    return jsonb_build_object('ok', false, 'error', 'notfound');\n  end if;\n\n  delete from public.quotes where id = qid;\n\n  return jsonb_build_object('ok', true, 'quotes', public.mtg_read_quotes());\nend;\n$$;\n\ngrant execute on function public.mtg_quote_delete(text, text, text) to anon, authenticated;\n",
  defaults: "-- Reset shop defaults (safe to re-run). Does NOT set staff PIN.\ninsert into public.shop(id, name, slogan, logo, phone, address)\nvalues (\n  1,\n  'Suman Thai Glass',\n  'Thai glass window & door calculator — see the size and estimate price with visuals',\n  'images/logo.svg',\n  '+8801727282829',\n  'Walia Bazar, Lalpur, Natore'\n)\non conflict (id) do update set\n  name = excluded.name,\n  slogan = excluded.slogan,\n  phone = excluded.phone,\n  address = excluded.address,\n  logo = coalesce(nullif(public.shop.logo, ''), excluded.logo);\n\ninsert into public.charges(id, net, extra) values (1, 200, 0)\non conflict (id) do update set net = excluded.net, extra = excluded.extra;\n\ninsert into public.cut_params(id) values (1)\non conflict (id) do nothing;\n"
};
